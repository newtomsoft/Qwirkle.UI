<UserControl x:Class="Qwirkle.UI.Wpf.Views.RackView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
            xmlns:viewModels="clr-namespace:Qwirkle.UI.Wpf.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:RackViewModel, IsDesignTimeCreatable=False}">
    <Grid>
        <StackPanel Orientation="Horizontal">
            <Button Style="{StaticResource RackButton}">
                <Image Source="{Binding TilesViewModel[0].Image}"/>
            </Button>
            <Button Style="{StaticResource RackButton}">
                <Image Source="{Binding TilesViewModel[1].Image}"/>
            </Button>
            <Button Style="{StaticResource RackButton}">
                <Image Source="{Binding TilesViewModel[2].Image}"/>
            </Button>
            <Button Style="{StaticResource RackButton}">
                <Image Source="{Binding TilesViewModel[3].Image}"/>
            </Button>
            <Button Style="{StaticResource RackButton}">
                <Image Source="{Binding TilesViewModel[4].Image}"/>
            </Button>
            <Button Style="{StaticResource RackButton}">
                <Image Source="{Binding TilesViewModel[5].Image}"/>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
